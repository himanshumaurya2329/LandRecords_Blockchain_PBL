# Optional overrides (leave empty or unset for automatic dynamic path discovery)
PORT=3001

# Dynamic Path Overrides (Automatically detected by app-root-path if omitted)
# PROJECT_ROOT=/path/to/LandRecords_extracted
# FABRIC_NETWORK_PATH=/path/to/fabric-samples/test-network
# FABRIC_SAMPLES_PATH=/path/to/fabric-samples
